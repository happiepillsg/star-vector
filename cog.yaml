build:
  gpu: true
  python_version: "3.10"
  python_packages:
    - packaging
    - torch==2.0.1
    - transformers==4.31.0
    - pillow
    - numpy
    - huggingface_hub
    - omegaconf
    - fairscale  # Add this for model loading
    - lxml
    - scikit-image
    - svgpathtools==1.6.1
    - accelerate
    - scipy==1.11.1
    - seaborn==0.12.2
    - sentencepiece==0.2.0
    # Additional dependencies that might be needed
    - einops
    - timm

predict: "predict.py:Predictor"
